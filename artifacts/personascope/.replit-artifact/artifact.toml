kind = "web"
previewPath = "/"
title = "PersonaScope"
version = "1.0.0"
id = "artifacts/personascope"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19017

[services.development]
run = "pnpm --filter @workspace/personascope run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/personascope", "run", "build" ]
publicDir = "artifacts/personascope/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19017"
BASE_PATH = "/"
